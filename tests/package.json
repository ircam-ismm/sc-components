{
  "name": "@ircam/sc-components-tests",
  "type": "module",
  "scripts": {
    "build": "webpack --config webpack.config.js",
    "watch": "webpack --config webpack.config.js --watch",
    "dev": "npm-run-all build --parallel start watch",
    "start": "serve . --config ./serve.json --listen 5000"
  }
}
